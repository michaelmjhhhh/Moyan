# 词典阅读器

一个读取用户本地词典包、帮助用户快速查词的桌面产品；默认不联网，只有用户主动发起版本检查时例外。

## Language

**词典阅读器**：
用户安装的软件本体；负责管理和读取本地词典包，本身不包含词典内容。
_Avoid_：词典、客户端

**词典包**：
用户自行取得并导入的一组词典数据，包括 `.mdx` 主文件及可选的 `.mdd`、CSS、图片、音频等配套资源。
_Avoid_：词典文件、MDX 文件

**词库**：
阅读器记录的全部词典包引用，以及用户为它们设置的启用状态和展示顺序。
_Avoid_：词典列表、仓库

**词条**：
一个词典包中以某个词头为入口的正文内容；同一查询可以在多个词典包中命中多个词条。
_Avoid_：搜索结果、释义

**离线原则**：
查词、索引、正文渲染和词典资源访问默认完全在本机完成；只有用户主动点击版本检查时，应用才访问更新服务。
_Avoid_：联网查词、后台联网、遥测
